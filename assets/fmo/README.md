The original PDF remains unchanged. `fotosentez.webp`, `fmo-a.webp`, and
`fmo-b.webp` are preserved because they carry scientific structure or argument.

The six `*-beylab.webp` files are new web-only editorial artwork generated for
the site's dark navy, white, violet, and magenta visual system. The earlier
magazine-derived images remain alongside them as a recoverable archive, but the
web article no longer references those versions.
