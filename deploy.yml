:root {
  --bg: #0b0d12;
  --bg-soft: #12151d;
  --card: #151a24;
  --border: #232a38;
  --text: #e8ebf2;
  --muted: #9aa3b5;
  --accent: #5ad1c4;
  --accent-2: #7c8cff;
  --max: 760px;
  --radius: 14px;
  --serif: "Iowan Old Style", "Palatino Linotype", "Georgia", serif;
  --sans: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
}
* { box-sizing: border-box; }
html { scroll-behavior: smooth; }
body {
  margin: 0;
  background: radial-gradient(1200px 600px at 50% -10%, #161b27 0%, var(--bg) 55%);
  color: var(--text);
  font-family: var(--sans);
  line-height: 1.65;
  -webkit-font-smoothing: antialiased;
}
a { color: var(--accent); text-decoration: none; }
a:hover { color: var(--accent-2); }
.wrap { max-width: var(--max); margin: 0 auto; padding: 0 22px; }
.site-header { padding: 46px 0 28px; text-align: center; border-bottom: 1px solid var(--border); }
.brand { font-size: 38px; font-weight: 800; letter-spacing: -0.5px; margin: 0; }
.brand a { color: var(--text); }
.brand .dot { color: var(--accent); }
.tagline { color: var(--muted); margin: 10px 0 18px; font-size: 15.5px; }
.nav { display: flex; gap: 18px; justify-content: center; font-size: 14.5px; }
.nav a { color: var(--muted); }
.nav a:hover { color: var(--accent); }
.post-list { padding: 36px 0 10px; list-style: none; margin: 0; }
.post-card { display: block; padding: 22px 0; border-bottom: 1px solid var(--border); }
.post-card .meta { font-size: 13px; color: var(--muted); text-transform: uppercase; letter-spacing: 0.6px; }
.post-card h2 { margin: 6px 0 8px; font-size: 23px; font-weight: 700; color: var(--text); line-height: 1.3; }
.post-card:hover h2 { color: var(--accent); }
.post-card p { margin: 0; color: var(--muted); font-size: 15.5px; }
.article { padding: 40px 0 20px; }
.article .back { font-size: 14px; color: var(--muted); display: inline-block; margin-bottom: 26px; }
.article h1 { font-size: 33px; line-height: 1.22; margin: 0 0 10px; letter-spacing: -0.5px; }
.article .meta { color: var(--muted); font-size: 14px; margin-bottom: 30px; }
.content { font-family: var(--serif); font-size: 19px; line-height: 1.78; }
.content h2 { font-family: var(--sans); font-size: 25px; margin: 38px 0 12px; }
.content h3 { font-family: var(--sans); font-size: 20px; margin: 30px 0 10px; }
.content p { margin: 0 0 22px; }
.content img { max-width: 100%; border-radius: var(--radius); }
.content blockquote { margin: 24px 0; padding: 4px 20px; border-left: 3px solid var(--accent); color: var(--muted); font-style: italic; }
.content code { background: var(--bg-soft); border: 1px solid var(--border); padding: 2px 6px; border-radius: 6px; font-size: 0.85em; }
.content pre { background: var(--bg-soft); border: 1px solid var(--border); padding: 16px; border-radius: var(--radius); overflow-x: auto; }
.content pre code { background: none; border: none; padding: 0; }
.content ul, .content ol { padding-left: 22px; margin: 0 0 22px; }
.content li { margin: 6px 0; }
.content hr { border: none; border-top: 1px solid var(--border); margin: 36px 0; }
.content a { border-bottom: 1px solid rgba(90,209,196,0.4); }
.site-footer { margin-top: 50px; padding: 30px 0 50px; border-top: 1px solid var(--border); text-align: center; color: var(--muted); font-size: 13.5px; }
.state { padding: 60px 0; text-align: center; color: var(--muted); }
@media (max-width: 600px) {
  .brand { font-size: 31px; }
  .article h1 { font-size: 27px; }
  .content { font-size: 18px; }
}
